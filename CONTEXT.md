# Corplink VPN

Corplink 为需要公司出口的目标建立 VPN 连接。日常运行需要区分用户希望保持连接、后台进程存活和隧道实际可用。

## Language

**VPN 运行意图**：用户希望保持连接或明确停止连接的选择。临时掉线不能等同于用户停止。

**VPN 运行代次**：一次启动及其恢复尝试共同所属的运行范围。重新启动产生新代次，旧代次的观测不能改变新代次的状态。

**VPN 连接会话**：一次选中 VPN 节点并完成认证、远端协商和隧道建立的连接过程。取配置的节点与隧道目标必须一致。

**VPN 网络资源**：连接取得的 WireGuard、TUN 或 netstack、系统路由和 DNS 状态。连接结束时需要释放本次取得的资源并恢复本次覆盖的设置。

**VPN 可用状态**：基于当前连接的有效观测得到的状态。隧道就绪、目标路由匹配和目标服务可访问是不同事实，进程存在不能代替这些判断。

**managed routes**：用户声明的 GitHub Meta 或 DNS host 来源所解析的 VPN 路由。解析结果、缓存结果和当前已应用结果是不同事实。

**认证会话状态**：可过期、可重新生成的登录及设备状态。它与用户维护的配置意图分别持久化。
